# 📅 2026-04-07 TIL

## 🎯 학습 주제

* HTML 시맨틱 구조
* CSS 선택자 및 박스 모델
* Flexbox / Grid / Position 레이아웃
* Form 입력 구조
* CSS 인터랙션

---

## 🧠 핵심 정리

* 시맨틱 태그(`header`, `main`, `section`, `footer`)로 구조 명확화
* CSS 선택자 우선순위(id > class > tag) 및 범위 이해
* 박스 모델(`margin`, `padding`, `border`) 기반 레이아웃 조정
* Flexbox(1차원) / Grid(2차원) 사용 목적 구분
* `position: relative/absolute` 기준 요소 개념 이해
* form의 `name` 속성은 서버 전달 데이터 key 역할

---

## ⚙️ 적용

* `div` 중심 구조를 시맨틱 구조로 리팩토링
* Flexbox / Grid를 활용한 레이아웃 구성
* `:hover`, `transition`을 활용한 인터랙션 적용
* form 요소 기반 사용자 입력 구조 구현

---

## 🔍 개선 포인트

* Flexbox와 Grid 사용 기준 명확화 필요
* position 사용 시 기준 요소 설정에 대한 이해 보완 필요
